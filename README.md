# NutriTrack Pro

Static PWA version of NutriTrack.

## GitHub Pages
Publish the `main` branch from the repository root.

The app entry point is `index.html`.

## Android
Open the published HTTPS URL in Chrome on Android and use the Chrome menu to install/add NutriTrack to the home screen.
